<?php/* Add this on all pages on top. */set_include_path($_SERVER['DOCUMENT_ROOT'].'/'.PATH_SEPARATOR.$_SERVER['DOCUMENT_ROOT'].'/library/classes/');/** * Standard includes */require_once 'config/database.php';require_once 'config/smarty.php';require_once 'Zend/Session.php';$zfsession = new Zend_Session_Namespace('MAILBOK_ADMIN');require_once 'class/admin.php';$adminObject	= new class_admin();if (!empty($_POST) && count($_POST) > 0) {	if(isset($_POST['username']) && isset($_POST['password'])) {			$username = (isset($_POST['username'])) ? $_POST['username'] : null;		$password = (isset($_POST['password'])) ? $_POST['password'] : null;		$adminData	= $adminObject->checkLogin($username, $password);		$message			= '';		if($adminData) {			// Clear the identity from the session			$zfsession->adminData = $zfsession->account = $zfsession->config = $zfsession->identity = $zfsession->config;			unset($zfsession->adminData, $zfsession->account, $zfsession->identity, $zfsession->config);			// Identity exists; store in session			$zfsession->adminData	= $adminData;			$zfsession->identity	= $zfsession->adminData['admin_code'];			$zfsession->config		= $config;						//session_write_close();			header("Location: /");			exit;					} else {			$message = 'You are not a valid system user';		}	} else {		$message = 'Please put in username and password';	}	$smarty->assign('loginmessage', $message);}/* Display the template */	$smarty->display('login.tpl');?>